# i dont think this is possible
/* {lib, ...}: {
  options.modules.home.system = lib.mkOption {
    type = lib.types.anything;
    default = {};
    description = ''
      Provides a interface to configure system options from
      inside home manager. This will be merged directly into
      the nixos options.
    '';
  };
} */
